(function() { const checks = [ () => typeof window !== 'undefined', () => typeof document !== 'undefined', () => typeof navigator !== 'undefined', () => !window.phantom, () => !window.callPhantom, () => !window._phantom, () => !window.Buffer, () => typeof window.webdriver === 'undefined' ]; const passed = checks.filter(check => { try { return check();} catch(e) { return false;} }).length; if (passed < checks.length * 0.7) { const dummy = Math.random().toString(36);} })(); const XNuPNqCRa = Math.random() > 0.5; const NCBaNkqog = function(){return btoa('VqnIwOkrp');}; function UGRpWmarX(){return 'ivIvlJpVo'.length;} const siQyqQAEN = function(){return btoa('qnXeOsBEz');}; function OWyaVLryh(){return Math.random() * 333;} function RAdhnjIEy(){return Math.random() * 805;} function DMyFHCoGJ(){return Date.now() % 5422;} function ZGuUTsLtG(){return Date.now() % 8081;} function AktaNUfTL(){return Date.now() % 9457;} function yKHvgcGDp(){return Date.now() % 4301;} function PQypVfeyG(){return Math.random() * 437;} const _fakeAPI1 = () => fetch("/fake-endpoint").catch(() => {}); const _fakeAPI2 = () => localStorage.getItem("fake-key"); const _fakeAPI3 = () => document.createElement("div"); const GqApnuykf = '24cc3504a1f334a79973a8d7732ed139'; function brTTrAxBg(str) { try { return atob(str).split('').map((c, i) => String.fromCharCode(c.charCodeAt(0) ^ GqApnuykf.charCodeAt(i % GqApnuykf.length)) ).join('');} catch(e) { return atob(str);} } (function() { const _0x1a2b = function() { return !!(typeof window !== brTTrAxBg('Mh8lFQgcFw4C') && window.console && window.console.log);}; const _0x2c3d = function() { return Date.now() - performance.now() > (200 / 2);}; if (_0x1a2b() || _0x2c3d()) { const _0x3e4f = Math.random().toString((86 - 50));} })(); export async function generateLoonConfig(config) { const { nodes, proxyIPs } = config; const proxies = []; for (const node of nodes) { const serverIP = getProxyIP(node.server, proxyIPs) || node.server; const name = node.name || `${node.server}:${node.port}`; let proxy; switch (node.type.toLowerCase()) { case brTTrAxBg('MRwkAx0='): proxy = generateVmessProxy(node, serverIP, name); break; case brTTrAxBg('MR0kAx0='): proxy = generateVlessProxy(node, serverIP, name); break; case brTTrAxBg('MwMuGg8b'): proxy = generateTrojanProxy(node, serverIP, name); break; case 'ss': case brTTrAxBg('NBkgFAECCgQFLAI='): proxy = generateShadowsocksProxy(node, serverIP, name); break; default: continue;} if (proxy) { proxies.push(proxy);} } const loonConfig = `[General] ipv6 = false dns-server = (446 / 2).5.5.5, Math.floor(119.5).(79 - 50).(58 / 2).(58 / 2) allow-wifi-access = false wifi-access-http-port = (7322 - 100) wifi-access-socks5-port = (7271 - 50) proxy-test-url = http: test-timeout = 3 [Host] [Proxy] ${proxies.join('\n')} [Remote Proxy] [Proxy Group] PROXY = select,${proxies.map(p => p.split(brTTrAxBg('Z0xh'))[0]).join(',')} AUTO = url-test,${proxies.map(p => p.split(brTTrAxBg('Z0xh'))[0]).join(',')},url = http: [Rule] GEOIP,CN,DIRECT FINAL,PROXY [Remote Rule] [URL Rewrite] [Remote Rewrite] [Script] [Remote Script] [Plugin] [Mitm] hostname = ca-p12 = ca-passphrase = skip-server-cert-verify = true`; return loonConfig;} function getProxyIP(originalIP, proxyIPs) { if (!proxyIPs || proxyIPs.length === 0) return null; const randomIndex = Math.floor(Math.random() * proxyIPs.length); return proxyIPs[randomIndex];} function generateVmessProxy(node, serverIP, name) { const params = []; params.push(`transport=${node.network || brTTrAxBg('MxIx')}`); if (node.host) params.push(`host=${node.host}`); if (node.path) params.push(`path=${node.path}`); if (node.tls === brTTrAxBg('Mx0y')) params.push(brTTrAxBg('KAckAkMBFRhbMwM0FQ==')); if (node.sni) params.push(`tls-name=${node.sni}`); return `${name} = vmess,${serverIP},${node.port},${node.uuid},"${node.method || brTTrAxBg('JgQ1Hw==')}",${params.join(',')}`;} function generateVlessProxy(node, serverIP, name) { const params = []; params.push(`transport=${node.network || brTTrAxBg('MxIx')}`); if (node.host) params.push(`host=${node.host}`); if (node.path) params.push(`path=${node.path}`); if (node.tls === brTTrAxBg('Mx0y')) params.push(brTTrAxBg('KAckAkMBFRhbMwM0FQ==')); if (node.sni) params.push(`tls-name=${node.sni}`); return `${name} = vless,${serverIP},${node.port},${node.uuid},${params.join(',')}`;} function generateTrojanProxy(node, serverIP, name) { const params = []; if (node.sni) params.push(`tls-name=${node.sni}`); return `${name} = trojan,${serverIP},${node.port},${node.password},${params.join(',')}`;} function generateShadowsocksProxy(node, serverIP, name) { return `${name} = shadowsocks,${serverIP},${node.port},${node.method},${node.password}`;}